// Layout constants shared across the application

/** Width offset for canvas area: toolbar (48px) + properties panel (212px) */
export const CANVAS_WIDTH_OFFSET = 260;

/** Height offset for canvas area: topbar (40px) + statusbar (40px) */
export const CANVAS_HEIGHT_OFFSET = 80;

/** Template panel width */
export const TEMPLATE_PANEL_WIDTH = 240;

/** Minimum zoom level (10%) */
export const MIN_ZOOM = 0.1;

/** Maximum zoom level (500%) */
export const MAX_ZOOM = 5.0;

/** Default padding for fit-to-content viewport calculations */
export const DEFAULT_VIEWPORT_PADDING = 50;

/** Angle snap increment for line/arrow tools (45 degrees in radians) */
export const ANGLE_SNAP_45_RAD = Math.PI / 4;

/** Angle snap increment for rotation (15 degrees) */
export const ROTATION_SNAP_DEG = 15;
