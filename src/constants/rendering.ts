// Rendering constants for canvas and export

/** Maximum dimension for imported images (maintains aspect ratio) */
export const MAX_IMPORTED_IMAGE_SIZE = 800;

/** Maximum items in image LRU cache */
export const IMAGE_CACHE_MAX_SIZE = 100;

/** Maximum items in video thumbnail cache */
export const VIDEO_CACHE_MAX_SIZE = 50;

/** Template preview thumbnail size in pixels */
export const TEMPLATE_PREVIEW_SIZE = 80;

/** Template preview padding in pixels */
export const TEMPLATE_PREVIEW_PADDING = 8;

/** Export preview padding in pixels */
export const EXPORT_PREVIEW_PADDING = 20;

/** Minimum text width for measurement */
export const MIN_TEXT_WIDTH = 20;

/** Default toast notification duration in milliseconds */
export const DEFAULT_TOAST_DURATION_MS = 3000;

/** Error toast notification duration in milliseconds */
export const ERROR_TOAST_DURATION_MS = 5000;

/** QuadTree max items per node */
export const QUADTREE_MAX_ITEMS = 10;

/** QuadTree max depth */
export const QUADTREE_MAX_DEPTH = 8;
